using UnityEngine;

namespace AICodingGame.Data
{
    public class LogManager : MonoBehaviour
    {
        public GameObject logPrefab;

        /*private void OnEnable()
        {
            foreach (var log in BattleLogger.ReadLogs())
            {
                //Instance()
            }
        }*/
    }
}